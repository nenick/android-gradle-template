steps:
  - task: Gradle@2
    displayName: output test ktlint
    inputs:
      tasks: 'ktlint-test'
      gradleWrapperFile: 'gradlew'
      publishJUnitResults: false
    condition: succeededOrFailed()