parameters:
  - name: 'modules'
    type: object

steps:
  - task: Bash@3
    displayName: Moule list check
    inputs:
      filePath: 'pipelines/templates/check-module-list.sh'
      arguments: ${{join(' ', parameters.modules)}}
    condition: succeededOrFailed()